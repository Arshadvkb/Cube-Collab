services:
   client:
     build: ./client
     ports:
         - "5173:5173"
     container_name: frontend
     restart: always
     

   server:
     build: ./server
     ports:
       - "5000:5000"
     container_name: backend
     restart: always 
    

     